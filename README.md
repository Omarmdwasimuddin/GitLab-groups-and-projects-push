# Docker-Desktop: heading

#### go--->http://localhost:8000/dashboard/home --->click: Groups --->click: New group --->click: Create group ---> Group name: group nam daw --->Visibility level: public daw --->click: Create group --->click: Create Project --->click: Create blank project ---> Project name daw--->Visibility Level: public --->Project Configuration e Initialize repository with a README er pash theke thik sign uthiye felo --->click: Create project

#### powershell open koro and command daw
```bash
mkdir gitlab
cd gitlab
npx create-next-app@latest
code .
```
---

#### project push koro
```bash
git init
git add .
git commit -m 'initial'
git remote add origin git@gitlab.com:wasuit-group1/wasimu.it/code-push.git
git branch -M main
git push -u origin main
```
---
> browser reload daw project push hoye jabe
